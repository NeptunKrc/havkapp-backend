export enum obligation_type_enum {
  mandatory = 'mandatory',
  volunteer = 'volunteer',
}

export enum activity_status_enum {
  upcoming = 'upcoming',
  ongoing = 'ongoing',
  completed = 'completed',
  cancelled = 'cancelled',
}
